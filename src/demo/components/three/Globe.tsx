"use client";
// The Enhanced view only ever renders the sky (view='sky'), so the demo's globe
// is never shown. Stubbed to avoid pulling in the demo's earth textures.
export default function Globe() {
  return null;
}
